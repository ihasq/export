{
  "name": "my-export-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "generate-export-types && wrangler dev",
    "export": "generate-export-types && wrangler deploy"
  },
  "dependencies": {
    "export-runtime": "^0.0.9"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
